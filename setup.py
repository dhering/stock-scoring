#!/usr/bin/env python

from distutils.core import setup

setup(name='stock-scoring',
      version='1.0',
      description='',
      author='Dennis Hering',
      author_email='hering.dennis@googlemail.com',
      packages=['', 'libs', 'model', 'downloader', 'scraper'],
     )